function Select() {
  return (
    <select>
      <option value="device/all">All</option>
      <option value="device/delete">Delete Device</option>
      <option value="device/add">Add Device</option>
    </select>
  );
}

export default Select;
